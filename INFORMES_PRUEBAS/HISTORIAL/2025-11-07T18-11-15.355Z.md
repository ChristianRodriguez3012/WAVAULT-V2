# Informe de Pruebas de Caja Blanca - Ejecución 2025-11-07T18:11:15.355Z

Archivo de pruebas: backend/tests/api.functional.test.js

Entorno: Jest + Supertest (dev container)

## Resumen ejecutivo

- CP-API-001 - Bloqueo de middleware de autorización: **PASÓ**
- CP-API-002 - Validación de procesamiento de archivos (demo con tag): **PASÓ**

---

## CP-API-001 - Bloqueo de middleware de autorización

Descripción: Verifica que un usuario con rol cliente no pueda acceder a endpoint exclusivo de productor (/subir-beat).

Resultado final: **PASÓ**

### Pasos de ejecución (ejecutados)
1. Obtener token con /auth/login para usuario rol cliente
   - Detalle: POST /auth/login { email: cliente@test.local } -> token obtenido: client-token
2. POST /subir-beat con header Authorization Bearer <token de cliente>
   - Detalle: Sin archivo adjunto (prueba de autorización)
   - Código HTTP: 403

### Resultado esperado
El servidor responde 401 Unauthorized o 403 Forbidden y niega el acceso a la ruta.

### Resultado observado
Respuesta HTTP 403 con body: {"error":"Forbidden"}

---

## CP-API-002 - Validación de procesamiento de archivos (demo con tag)

Descripción: Verificar que al subir un beat, el servidor procese y genere un demo que contenga el tag sonoro.

Resultado final: **PASÓ**

### Pasos de ejecución (ejecutados)
1. Obtener token con /auth/login para usuario rol productor
   - Detalle: POST /auth/login { email: productor@test.local } -> token obtenido: producer-token
2. POST /subir-beat con archivo adjunto (beat.mp3) y Authorization Bearer <token>
   - Detalle: attach -> /workspaces/WAVAULT-V2/backend/tests/fixtures/beat.mp3 -> respuesta inicial: 201
   - Código HTTP: 201
3. Esperar procesamiento y verificar existencia del demo en el sistema de archivos
   - Detalle: Verificar ruta: /tmp/wavault-demos/beat-demo.mp3

### Resultado esperado
Se genera un archivo demo (beat-demo.mp3) en el sistema de archivos que contiene el tag sonoro.

### Resultado observado
Demo encontrado: /tmp/wavault-demos/beat-demo.mp3, tag presente: true

---

### Detalles (JSON)

```json
[
  {
    "id": "CP-API-001",
    "title": "Bloqueo de middleware de autorización",
    "description": "Verifica que un usuario con rol cliente no pueda acceder a endpoint exclusivo de productor (/subir-beat).",
    "expected": "El servidor responde 401 Unauthorized o 403 Forbidden y niega el acceso a la ruta.",
    "steps": [
      {
        "action": "Obtener token con /auth/login para usuario rol cliente",
        "detail": "POST /auth/login { email: cliente@test.local } -> token obtenido: client-token"
      },
      {
        "action": "POST /subir-beat con header Authorization Bearer <token de cliente>",
        "detail": "Sin archivo adjunto (prueba de autorización)",
        "code": 403
      }
    ],
    "passed": true,
    "notes": null,
    "statusCode": 403,
    "observed": "Respuesta HTTP 403 con body: {\"error\":\"Forbidden\"}"
  },
  {
    "id": "CP-API-002",
    "title": "Validación de procesamiento de archivos (demo con tag)",
    "description": "Verificar que al subir un beat, el servidor procese y genere un demo que contenga el tag sonoro.",
    "expected": "Se genera un archivo demo (beat-demo.mp3) en el sistema de archivos que contiene el tag sonoro.",
    "steps": [
      {
        "action": "Obtener token con /auth/login para usuario rol productor",
        "detail": "POST /auth/login { email: productor@test.local } -> token obtenido: producer-token"
      },
      {
        "action": "POST /subir-beat con archivo adjunto (beat.mp3) y Authorization Bearer <token>",
        "detail": "attach -> /workspaces/WAVAULT-V2/backend/tests/fixtures/beat.mp3 -> respuesta inicial: 201",
        "code": 201
      },
      {
        "action": "Esperar procesamiento y verificar existencia del demo en el sistema de archivos",
        "detail": "Verificar ruta: /tmp/wavault-demos/beat-demo.mp3"
      }
    ],
    "passed": true,
    "notes": null,
    "demoPath": "/tmp/wavault-demos/beat-demo.mp3",
    "tagFound": true,
    "statusCode": 201,
    "observed": "Demo encontrado: /tmp/wavault-demos/beat-demo.mp3, tag presente: true"
  }
]
```
