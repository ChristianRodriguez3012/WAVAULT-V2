# Informe E2E - Ejecución 2025-11-07T18:18:28.354298

## Resumen


---

### Detalles JSON

```json
[]
```

# ejemplo (al final del test)
import conftest

conftest.run_results.append({
  "id": id,
  "title": title,
  "passed": passed,
  "steps": steps,
  "observed": observed,
  "notes": notes
})

# desde /workspaces/WAVAULT-V2/e2e
source .venv/bin/activate
pytest -q --capture=no

# abrir último informe en el host browser (usa $BROWSER del devcontainer)
LATEST=$(ls -t INFORMES_PRUEBAS/E2E/HISTORIAL | head -n1)
$BROWSER "file://$(pwd)/INFORMES_PRUEBAS/E2E/HISTORIAL/$LATEST"
